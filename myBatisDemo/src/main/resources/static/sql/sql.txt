create table product(
pno int not null auto_increment,
pname varchar(50) not null,
kind varchar(50) not null,
price int not null default 0,
in_date datetime not null default current_timestamp(),
up_date datetime not null default current_timestamp(),
primary key (pno)
);


insert into product(pname, kind, price)
values
('스프링부트', '도서', 30000),
('자바', '도서', 20000),
('냉장고', '생활가전', 309000)
;

<!--mybatis.org -> myBatis3 docs -> Getting Started -> Exploring Mapped SQL Statements
 -->

create table product_log(
    logid int not null auto_increment,
    pno int null default '0',
    price int null default '0',
    in_date datetime null default current_timestamp(),
    primary key (logid)
)
comment = '상품로그'
;

동적쿼리 union all 활용
select * from product_log;
insert into product_log(pno, price) select 2, 1111 from dual union all select 2, 1111 from dual union all select 2, 1111 from dual;